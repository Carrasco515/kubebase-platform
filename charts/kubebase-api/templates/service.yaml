apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubebase-api.fullname" . }}
  labels:
    {{- include "kubebase-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kubebase-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
