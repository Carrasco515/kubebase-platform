apiVersion: v1
kind: Service
metadata:
  name: kubebase-api
  labels:
    app.kubernetes.io/name: kubebase-platform
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kubebase-platform
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
