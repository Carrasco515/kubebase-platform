[1mdiff --git a/README.md b/README.md[m
[1mindex 9623e08..87608d5 100644[m
[1m--- a/README.md[m
[1m+++ b/README.md[m
[36m@@ -12,6 +12,14 @@[m [mnon-secret configuration and an example Secret to show the pattern. Everything i[m
 designed to run on a **local cluster** (minikube, kind or Docker Desktop) and is[m
 validated by a read-only CI pipeline.[m
 [m
[32m+[m[32m> **For recruiters & reviewers:** This is a hands-on learning project that takes[m
[32m+[m[32m> one small app through a realistic delivery chain — **Docker → Kubernetes →[m
[32m+[m[32m> Kustomize → Helm → GitOps (Argo CD) → observability** — with read-only CI and[m
[32m+[m[32m> documentation at each step. It runs entirely on a local cluster, uses no real[m
[32m+[m[32m> secrets, and keeps "prod" as a clearly-labelled local example. It shows[m
[32m+[m[32m> *junior-level DevOps / Platform Engineering* fundamentals end to end, not a[m
[32m+[m[32m> production system.[m
[32m+[m
 ## Why I built it[m
 [m
 After building [CloudBase Lab](https://github.com/Carrasco515/cloudbase-lab) — my[m
[36m@@ -20,16 +28,37 @@[m [mproperly. KubeBase Platform is my hands-on way to understand how an app moves fr[m
 a container into a cluster: namespaces, Deployments, Services, configuration,[m
 health probes, resource limits and a clean manifest layout I can grow over time.[m
 [m
[31m-## What it demonstrates[m
[31m-[m
[31m-- **Containerising an app** with a small, non-root Dockerfile[m
[31m-- **Kubernetes core objects** — Namespace, Deployment, Service, ConfigMap, Secret[m
[31m-- **Health probes** (readiness + liveness) and **resource requests/limits**[m
[31m-- **Configuration management** — non-secret config via ConfigMap, secrets kept[m
[31m-  out of Git (only an example Secret is tracked)[m
[31m-- **Kustomize** for assembling the base deployment[m
[31m-- **CI** with GitHub Actions: Python syntax check, required-file checks and a[m
[31m-  `kubectl kustomize` build — no deployment to a real cluster[m
[32m+[m[32m## DevOps skills demonstrated[m
[32m+[m
[32m+[m[32m- **Containerization** — small, non-root Dockerfile on `python:3.12-slim`.[m
[32m+[m[32m- **Kubernetes manifests** — Namespace, Deployment (readiness + liveness probes,[m
[32m+[m[32m  resource requests/limits), Service and ConfigMap.[m
[32m+[m[32m- **Kustomize overlays** — a shared `base` with `dev` / `prod` overlays that patch[m
[32m+[m[32m  config and replica counts without duplication.[m
[32m+[m[32m- **Helm packaging** — the same app as a templated chart with `values-dev.yaml` /[m
[32m+[m[32m  `values-prod.yaml` profiles.[m
[32m+[m[32m- **GitOps with Argo CD** — `Application` manifests; the dev Application was[m
[32m+[m[32m  reconciled locally to **Synced & Healthy** (prod left as example-only).[m
[32m+[m[32m- **CI validation** — read-only GitHub Actions: Python compile, Kustomize build,[m
[32m+[m[32m  Helm lint/template, GitOps + observability checks. No deployment, no cluster.[m
[32m+[m[32m- **Observability** — a Prometheus-style `/metrics` endpoint, pod scrape[m
[32m+[m[32m  annotations and a Grafana starter dashboard.[m
[32m+[m[32m- **Operations documentation** — runbook-style docs for build, deploy, inspect[m
[32m+[m[32m  and safe cleanup.[m
[32m+[m[32m- **Security hygiene & safe defaults** — runs as non-root, no real secrets in Git,[m
[32m+[m[32m  manual GitOps sync, and "prod" kept as a clearly-labelled local example.[m
[32m+[m
[32m+[m[32m## Tech stack[m
[32m+[m
[32m+[m[32m| Area | Tools |[m
[32m+[m[32m|---|---|[m
[32m+[m[32m| App | Python, FastAPI, uvicorn |[m
[32m+[m[32m| Container | Docker |[m
[32m+[m[32m| Orchestration | Kubernetes (local: Minikube / kind / Docker Desktop) |[m
[32m+[m[32m| Config / templating | Kustomize, Helm |[m
[32m+[m[32m| GitOps | Argo CD |[m
[32m+[m[32m| Observability | Prometheus client (`/metrics`), Grafana (dashboard JSON) |[m
[32m+[m[32m| CI | GitHub Actions (read-only validation) |[m
 [m
 ## How it complements CloudBase Lab[m
 [m
[36m@@ -38,7 +67,8 @@[m [mhealth probes, resource limits and a clean manifest layout I can grow over time.[m
 | Orchestration | Docker Compose | Kubernetes |[m
 | Focus | Self-hosted homelab stack | Platform / app-on-Kubernetes lab |[m
 | Config & secrets | `.env` files | ConfigMap + Secret |[m
[31m-| Updates | Watchtower (opt-in) | (roadmap: Helm + GitOps) |[m
[32m+[m[32m| Packaging / delivery | Compose file | Kustomize → Helm → Argo CD GitOps |[m
[32m+[m[32m| Observability | container logs | logs + Prometheus `/metrics` |[m
 [m
 CloudBase Lab shows I can run a real multi-service stack; KubeBase Platform shows[m
 I'm building the **Kubernetes and Platform Engineering** skills on top of that.[m
[36m@@ -69,10 +99,30 @@[m [mkubebase-platform/[m
 ├── gitops/                  # GitOps (Argo CD) examples[m
 │   ├── README.md[m
 │   └── argocd/              # Argo CD Application manifests (dev + prod example)[m
[31m-├── docs/                    # Architecture, operations, GitOps[m
[32m+[m[32m├── observability/[m
[32m+[m[32m│   └── grafana/            # Grafana starter dashboard (JSON)[m
[32m+[m[32m├── docs/                    # architecture, operations, gitops, observability[m
[32m+[m[32m│   └── images/             # screenshots (optional — add your own)[m
 └── .github/workflows/ci.yml # Read-only validation[m
 ```[m
 [m
[32m+[m[32m## Architecture[m
[32m+[m
[32m+[m[32m```mermaid[m
[32m+[m[32mflowchart LR[m
[32m+[m[32m    A[FastAPI app<br/>app/main.py] --> B[Docker image][m
[32m+[m[32m    B --> C[Kubernetes base<br/>Deployment · Service · ConfigMap][m
[32m+[m[32m    C --> D[Kustomize overlays<br/>dev · prod][m
[32m+[m[32m    C --> E[Helm chart<br/>charts/kubebase-api][m
[32m+[m[32m    E --> F[Argo CD GitOps<br/>dev synced · prod example][m
[32m+[m[32m    A -. exposes .-> G[/metrics → Prometheus → Grafana/][m
[32m+[m[32m```[m
[32m+[m
[32m+[m[32mThe app is built once and delivered through progressively higher-level tooling:[m
[32m+[m[32mraw Kubernetes manifests, then Kustomize overlays, then a Helm chart, then GitOps[m
[32m+[m[32mwith Argo CD — with a Prometheus-style `/metrics` endpoint for observability. See[m
[32m+[m[32m[`docs/architecture.md`](docs/architecture.md) for the full breakdown.[m
[32m+[m
 ## The app[m
 [m
 A minimal FastAPI service with three endpoints:[m
[36m@@ -230,6 +280,22 @@[m [mdocker rm -f kubebase-api-metrics-test   # removes only this test container[m
 See [`docs/observability.md`](docs/observability.md) for what `/metrics` exposes,[m
 how Prometheus/Grafana would use it, and the future `kube-prometheus-stack` path.[m
 [m
[32m+[m[32m## Screenshots[m
[32m+[m
[32m+[m[32m> _Optional — these are placeholders. The project is fully usable without them;[m
[32m+[m[32m> screenshots can be added later by dropping PNGs into `docs/images/`._[m
[32m+[m
[32m+[m[32m| View | Image |[m
[32m+[m[32m|---|---|[m
[32m+[m[32m| README / project overview | `docs/images/kubebase-readme-preview.png` |[m
[32m+[m[32m| GitHub Actions CI (green) | `docs/images/github-actions-ci.png` |[m
[32m+[m[32m| Argo CD dev app — Synced & Healthy | `docs/images/argocd-dev-healthy.png` |[m
[32m+[m[32m| `/metrics` endpoint output | `docs/images/metrics-endpoint.png` |[m
[32m+[m
[32m+[m[32m<!-- Once added, embed them like:[m
[32m+[m[32m![Argo CD dev app Synced and Healthy](docs/images/argocd-dev-healthy.png)[m
[32m+[m[32m-->[m
[32m+[m
 ## Roadmap[m
 [m
 - [x] **Phase 1** — FastAPI demo app, Dockerfile, base Kubernetes manifests,[m
[1mdiff --git a/docs/architecture.md b/docs/architecture.md[m
[1mindex 1c822bd..9a44421 100644[m
[1m--- a/docs/architecture.md[m
[1m+++ b/docs/architecture.md[m
[36m@@ -104,40 +104,60 @@[m [mThis is the **Phase 5 observability layer**: the app and manifests are made[m
 ## CI[m
 [m
 A GitHub Actions workflow ([`.github/workflows/ci.yml`](../.github/workflows/ci.yml))[m
[31m-validates the project on every push/PR — **read-only**: it checks Python syntax,[m
[31m-verifies required files exist, confirms Kubernetes YAML is present and builds the[m
[31m-Kustomize base with `kubectl kustomize`. It does **not** build/push images or[m
[31m-deploy to any cluster.[m
[31m-[m
[31m-## Future Helm and GitOps path[m
[31m-[m
[31m-The project demonstrates a realistic progression of delivery tooling:[m
[31m-[m
[31m-```[m
[31m-App (FastAPI) -> Docker image -> Kubernetes manifests -> Kustomize overlays -> Helm chart -> GitOps (Argo CD)[m
[32m+[m[32mvalidates the project on every push/PR — **read-only**. It:[m
[32m+[m
[32m+[m[32m- checks Python syntax (`py_compile`) and that required files exist,[m
[32m+[m[32m- builds the Kustomize **base, dev and prod** with `kubectl kustomize`,[m
[32m+[m[32m- runs `helm lint` and renders the chart for **default, dev and prod** values,[m
[32m+[m[32m- structure/parse-checks the GitOps Argo CD `Application` manifests,[m
[32m+[m[32m- verifies the observability wiring (the `/metrics` route, the `prometheus-client`[m
[32m+[m[32m  dependency, and that scrape annotations render), and[m
[32m+[m[32m- validates the Grafana dashboard JSON if present.[m
[32m+[m
[32m+[m[32mIt does **not** build/push images, contact a cluster, or deploy anything.[m
[32m+[m
[32m+[m[32m## Delivery progression (Kustomize → Helm → GitOps)[m
[32m+[m
[32m+[m[32mThe project deliberately delivers the **same app** through progressively[m
[32m+[m[32mhigher-level tooling — the realistic path a Platform Engineering team follows:[m
[32m+[m
[32m+[m[32m```mermaid[m
[32m+[m[32mflowchart LR[m
[32m+[m[32m    A[FastAPI app] --> B[Docker image][m
[32m+[m[32m    B --> C[Kubernetes base<br/>Deployment · Service · ConfigMap][m
[32m+[m[32m    C --> D[Kustomize overlays<br/>dev · prod][m
[32m+[m[32m    C --> E[Helm chart<br/>charts/kubebase-api][m
[32m+[m[32m    E --> F[Argo CD GitOps<br/>dev synced · prod example][m
[32m+[m[32m    A -. /metrics .-> G[Prometheus → Grafana][m
 ```[m
 [m
[31m-- **Kustomize** (Phase 2) — raw manifests plus `dev`/`prod` overlays that patch[m
[31m-  the base's config and replica count without duplicating it.[m
[31m-- **Helm** (Phase 3) — the same app packaged as a chart (`charts/kubebase-api`)[m
[31m-  with `values.yaml` and `values-dev.yaml` / `values-prod.yaml` profiles. Helm[m
[31m-  is the **packaging/templating** path; Kustomize stays as the raw-manifest[m
[31m-  learning path. Both deploy the same app (don't run both into one namespace at[m
[31m-  once). The chart's example Secret is disabled by default (placeholders only).[m
[31m-- **GitOps / Argo CD** (Phase 4) — example `Application` manifests[m
[31m-  ([`gitops/argocd/`](../gitops/argocd/)) that deploy the Helm chart declaratively[m
[31m-  with Git as the source of truth. Sync is manual by default; prod is example[m
[31m-  only. See [`gitops.md`](gitops.md). These are preparation manifests — they do[m
[31m-  nothing until Argo CD is installed.[m
[31m-[m
[31m-Planned evolution from here:[m
[31m-[m
[31m-1. **More overlays / chart values** — additional per-environment differences[m
[31m-   (image tags, extra config), and possibly a `staging` profile.[m
[31m-2. **Run GitOps for real** — install Argo CD locally and let it reconcile the[m
[31m-   dev Application (optionally enabling automated sync once understood).[m
[31m-3. **Ingress + TLS and monitoring** — expose the Service through an Ingress[m
[31m-   controller and add Prometheus/Grafana observability.[m
[32m+[m[32m- **Kustomize overlays** (Phase 2) — the base manifests plus `dev`/`prod`[m
[32m+[m[32m  overlays ([`kubernetes/overlays/`](../kubernetes/overlays/)) that patch config[m
[32m+[m[32m  and replica count without duplicating the base.[m
[32m+[m[32m- **Helm chart** (Phase 3) — the same app packaged as a chart[m
[32m+[m[32m  ([`charts/kubebase-api`](../charts/kubebase-api/)) with `values.yaml` and[m
[32m+[m[32m  `values-dev.yaml` / `values-prod.yaml` profiles. Helm is the[m
[32m+[m[32m  packaging/templating path; Kustomize stays as the raw-manifest path. Both[m
[32m+[m[32m  deploy the same app — don't run both into one namespace at once. The chart's[m
[32m+[m[32m  example Secret is disabled by default (placeholders only).[m
[32m+[m[32m- **GitOps / Argo CD** (Phase 4) — `Application` manifests[m
[32m+[m[32m  ([`gitops/argocd/`](../gitops/argocd/)) deploy the Helm chart declaratively with[m
[32m+[m[32m  Git as the source of truth. Sync is manual by default. The **dev** Application[m
[32m+[m[32m  was reconciled locally to **Synced & Healthy**; **prod is example-only** and was[m
[32m+[m[32m  not applied. See [`gitops.md`](gitops.md).[m
[32m+[m[32m- **Observability** (Phase 5) — the `/metrics` endpoint, pod scrape annotations[m
[32m+[m[32m  and a Grafana **starter dashboard** placeholder[m
[32m+[m[32m  ([`observability/grafana/kubebase-api-dashboard.json`](../observability/grafana/kubebase-api-dashboard.json)),[m
[32m+[m[32m  generic and safe (no hardcoded Prometheus URL). See [`observability.md`](observability.md).[m
[32m+[m
[32m+[m[32mPlanned next (Phase 6):[m
[32m+[m
[32m+[m[32m1. **Ingress + TLS** — expose the Service through an Ingress controller instead of[m
[32m+[m[32m   `kubectl port-forward`.[m
[32m+[m[32m2. **A real monitoring stack** — install `kube-prometheus-stack` and import the[m
[32m+[m[32m   Grafana dashboard, optionally adding a `ServiceMonitor`/`PodMonitor`.[m
[32m+[m[32m3. **More profiles** — e.g. a `staging` overlay/values set, and per-environment[m
[32m+[m[32m   image tags.[m
 [m
 This mirrors a realistic progression from raw manifests → Kustomize → Helm →[m
[31m-GitOps that Platform Engineering teams use in practice.[m
[32m+[m[32mGitOps → observability that platform teams use in practice.[m
